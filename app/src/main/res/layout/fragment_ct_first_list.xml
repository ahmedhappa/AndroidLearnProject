<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- transitionGroup="true"  in order to ensure that the
    MaterialElevationScale transition is applied to the home screen as a whole,
    instead of to each of the individual views in the hierarchy -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_ct_first_screen"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:transitionGroup="true"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_ct_first_screen" />

</androidx.constraintlayout.widget.ConstraintLayout>